# Copy this file to .env.local and fill in the live Render backend URL once deployed
# (PLAN.md §9 Phase 4). Leave unset to run against the bundled Tecnomania fixture only —
# see src/lib/api.ts for the fallback behavior.

VITE_API_URL=
